# in this file we will more learning about strings

# string are array, we can use square brackets for access element of string(with value count from 0)
x = "alma still learning"
print(x[2])

# now we can loop use for loops
for x in "al majid":
    print(x)

# we can count length use len() function
y = "building the dream"
print(len(y))
